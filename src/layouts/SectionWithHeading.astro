---
interface Props {
  heading: string;
}

const { heading } = Astro.props;
---

<section class="max-w-2xl my-0 mx-auto pb-16">
  <h1 class="mb-6 text-[28px] font-bold">{heading}</h1>
  <slot />
</section>
